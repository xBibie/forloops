//even-odd-reporter
for (let i = 0; i <= 20; i++) {
    console.log(`Is ${i} even or odd?`);
  };
  if (i = even){
      console.log ('its even')
  } else {
      console.log ('its odd')
  };

//multiplication-tables

for (let i = 9; i+9) {
    const (i*9);
    return result
  };


for (let i = 1; i++) {
    const (i*1)
    if result => 10 start at 1
  };

  //grade-assigner

  function assignGrade(score) {
    if (score > 90) {
      return "A";
    } else if (score > 80) {
      return "B";
    } else if (score > 70) {
      return "C";
    } else if (score > 65) {
      return "D";
    } else {
      return "E";
    }
  }